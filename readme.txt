Download git bash for windows
go to file explorar where the project is saved (for e.g C:\JAVA\first-project) then right click and open git bash
git init .
create .gitignore file to ignore all except src and pom.xml
git add .
git commit -m "initial commit"
git remote add origin https://github.com/vibgyorvip/first-maven-project.git
git push -u origin master
Authentication will be required via mail id.
